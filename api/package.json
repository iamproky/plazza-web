{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "bun test",
    "bun:install": "npm install -g bun",
    "postinstall": "prisma migrate deploy && prisma generate && bun run --watch src/index.ts",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.0.2",
    "@elysiajs/swagger": "^1.0.5",
    "@prisma/client": "5.15.1",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^5.15.1"
  },
  "module": "src/index.js"
}